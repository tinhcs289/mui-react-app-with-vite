import { Form as FormRegister } from "./Form";
export default FormRegister;

export type {
  FormContextValues as FormRegisterContextValues,
  FormProps as FormRegisterProps,
  FormStates as FormRegisterStates,
  FormValues as FormRegisterValues,
} from "./types";
